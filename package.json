{
  "name": "Todo-App",
  "version": "1.0.0",
  "description": "Todo-app para projeto da pós graduação",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "api": "json-server --watch db.json",
    "serve": "run-p start api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas Henrick Serpa de Oliveira",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.17.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "bootstrap-icons": "^1.9.1"
  }
}
